README.md
bin
{{/* Shell resources are scoped by identity, not by hostname: their contents are
     "any personal machine" and "the work machine" rather than one named box. */}}
{{ if ne .profile "work" }}
.config/zsh/resources/work.zsh
{{ end }}
{{ if ne .profile "personal" }}
.config/zsh/resources/personal.zsh
{{ end }}
{{ if not .gui }}
.config/ghostty
.config/herdr
.config/native-shortcuts-herd
.config/zed
{{ end }}
{{/* Execs into zsh when chsh could not change the login shell; Linux only. */}}
{{ if ne .chezmoi.os "linux" }}
.bash_profile
{{ end }}
