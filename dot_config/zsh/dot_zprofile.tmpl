{{ if eq .chezmoi.os "darwin" -}}
eval "$(/opt/homebrew/bin/brew shellenv)"
{{- else -}}
# Servers use mise (activated in .zshrc) rather than Homebrew; mise-managed
# binaries are reached via its shims, and ~/.local/bin is added there too.
{{- end }}
