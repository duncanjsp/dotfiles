packages:
  brews:
    - atuin
    - bat
    - carapace
    - chezmoi
    - direnv
    - eza
    - fastfetch
    - fd
    - gh
    - git
    - go
    - gum
    - herdr
    - jandedobbeleer/oh-my-posh/oh-my-posh
    - jj
    - mise
    - neovim
    - node@20
    - prettier
    - ripgrep
    - stylua
    # Used directly by nvim-treesitter, not just as a neovim dependency.
    - tree-sitter
    - umputun/apps/revdiff
  casks:
    - 1password-cli
  # Linux uses mise: user-local, no sudo, and the same on Debian-based and
  # immutable distros alike.
  #
  # Omits git (from the distro) and mise (installs itself). neovim@latest
  # resolves to nightly, hence @stable. eza and revdiff are not in mise's
  # registry but publish Linux binaries, so they come from releases via ubi.
  mise:
    core:
      - atuin@latest
      - bat@latest
      - carapace@latest
      - chezmoi@latest
      - direnv@latest
      - fastfetch@latest
      - fd@latest
      - gh@latest
      - go@latest
      - gum@latest
      - jj@latest
      - neovim@stable
      - node@latest
      - npm:prettier@latest
      - oh-my-posh@latest
      - ripgrep@latest
      - stylua@latest
      - tree-sitter@latest
      - ubi:eza-community/eza@latest
      - ubi:umputun/revdiff@latest
    gui:
      - herdr@latest
